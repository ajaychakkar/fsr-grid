import { HeaderCellDirective } from './header-cell.directive';

describe('HeaderCellDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderCellDirective();
    expect(directive).toBeTruthy();
  });
});
