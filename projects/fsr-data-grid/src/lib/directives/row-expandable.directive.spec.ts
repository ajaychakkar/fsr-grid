import { RowExpandableDirective } from './row-expandable.directive';

describe('RowExpandableDirective', () => {
  it('should create an instance', () => {
    const directive = new RowExpandableDirective();
    expect(directive).toBeTruthy();
  });
});
